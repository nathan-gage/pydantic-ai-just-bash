[project]
name = "pydantic-ai-just-bash"
version = "0.1.0"
description = "Pydantic AI Toolset for just-bash"
readme = "README.md"
authors = [
    { name = "Nathan Gage", email = "contact@nathangage.co" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
pydantic-ai-just-bash = "pydantic_ai_just_bash:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
